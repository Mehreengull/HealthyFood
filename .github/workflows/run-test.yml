name: run unit test practics
on:
  push:
    branches:
      'feature/*'


  jobs:
  run-test:
      runs-on: windows-latest

      steps:
      - name: Checkout
        uses: actions/checkout@v3.5.3
      -name: run unit test
        run: run test
         
      
